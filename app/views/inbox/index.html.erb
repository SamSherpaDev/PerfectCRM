<% content_for :title, "Inbox · PerfectCRM" %>
<div class="page">
  <%= render layout: "shared/page_header", locals: { eyebrow: "Conversations", title: "Inbox", subtitle: "Every client message in one place, oldest waiting reply first." } do %><% end %>
  <%= render "shared/flash" %>

  <%= render "shared/empty_state", title: "No conversations yet", body: "Client messages will land here in one thread per traveler, so every reply starts with the full story.", scene: :teahouse %>
</div>
