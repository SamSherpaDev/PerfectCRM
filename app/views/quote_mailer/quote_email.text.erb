Hi <%= @quote.owner_name %>,

Here is your quote <%= @quote.reference %> for <%= @quote.subject_label %><% if @quote.departure_label.present? %> (<%= @quote.departure_label %>)<% end %>.

<% @quote.lines.each do |line| %>
- <%= line.description %> x <%= line.quantity %> — <%= money_in(line.total_minor, @quote.currency) %>
<% end %>

Total <%= money_in(@quote.subtotal_minor, @quote.currency) %><% if @quote.party_size %> for <%= pluralize(@quote.party_size, "guest") %><% end %><% if @quote.valid_until %> · valid until <%= date_short(@quote.valid_until) %><% end %>.
<% if @quote.included.present? %>
What is included
<%= @quote.included %>
<% end %>
<% if @quote.notes.present? %>
A note from Sam
<%= @quote.notes %>
<% end %>

The full quote is attached as a PDF. To accept, tap this link — no account needed:
<%= @accept_url %>

Questions? Just reply to this email.
<% if @signature_text.present? %>
<%= @signature_text %>
<% end %>
