<%= turbo_stream.update "draft-status" do %><% if @upload_refused %><%= Outbound::Uploads::REFUSAL %><% elsif @saved %>Draft saved just now.<% else %>Draft cleared.<% end %><% end %>
<%= turbo_stream.replace "draft-attachments", partial: "reply_box/attachments", locals: { draft: @draft } %>
