<%# Slot: PerfectBook booking cards (crm-pb-link). %>
<%= render "shared/perfectbook_bookings", record: client,
  bookings: perfectbook_bookings_for(client), refresh_path: refresh_bookings_client_path(client) %>
